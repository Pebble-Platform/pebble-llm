---
title: Specs Table of Contents
kind: specs-index
generated-by: /init-project
last-updated: {{YYYY-MM-DD}}
---

# Specs Table of Contents

<!-- Navigational index for AI agents and human contributors. Lets a
     reader route to the right feature spec without loading every
     file. Regenerated by `/init-project` on each run.

     For AI agents: this file is safe to load first. Use the Quick
     Reference table to pick the spec(s) most relevant to the user's
     question, then load only those. The Relationships Graph below
     lets you traverse depends-on edges between features. -->

## Table of Contents

- [Quick Reference](#quick-reference)
- [By Domain / Area](#by-domain--area)
- [Relationships Graph](#relationships-graph)
- [Unresolved Clarifications](#unresolved-clarifications)

## Quick Reference

<!-- One row per feature spec. Populate from the frontmatter and
     Part A / Part B of each `specs/{slug}.md`. Keep Intent to a
     single line (trim from the spec's Intent > What field).

     Lenses column uses letter codes for brevity:
       S = Structure & Architecture
       R = Request Flow
       D = Data Flow
       X = Dependencies & Relationships
       A = API Surface
     Each letter appears if the lens is populated; use `-` for
     `not applicable`. Example: `S R D X A` = all five lenses
     populated; `S - D X -` = Request Flow and API Surface marked
     not applicable. -->

| Slug | Title | Status | Intent (one line) | REQs | NFRs | ACs | Lenses |
|------|-------|--------|-------------------|------|------|-----|--------|
| [{{slug}}]({{slug}}.md) | {{Feature Name}} | {{draft\|active\|deprecated}} | {{one-line intent}} | {{n}} | {{n}} | {{n}} | {{S R D X A}} |

## By Domain / Area

<!-- Group specs by the domain/area they serve. Derive from each
     spec's Dependencies & Relationships > Cross-cutting domains and
     flows bucket, plus visible folder grouping if specs are nested.
     Omit this section if there is only one domain. -->

### {{Domain name}}
- [{{slug}}]({{slug}}.md) -- {{one-line role in the domain}}

## Relationships Graph

<!-- Feature-to-feature dependency edges derived from each spec's
     Dependencies & Relationships > Depends on features (upstream).
     Use the arrow form `A -> B` meaning "A depends on B". Agents
     traverse this graph to find transitive context.

     Mark cycles or ambiguous edges explicitly; do not silently
     break them. -->

- {{slug-a}} -> {{slug-b}}, {{slug-c}}
- {{slug-b}} -> {{slug-d}}

<!-- Optional: a second section for reverse edges, useful for
     blast-radius reasoning. Derived from Depended on by features. -->

**Reverse (depended-on-by):**
- {{slug-b}} <- {{slug-a}}
- {{slug-d}} <- {{slug-b}}

## Unresolved Clarifications

<!-- Per-spec count of `[NEEDS CLARIFICATION]` markers so an agent
     knows which specs are partially stubbed. Cite total across all
     specs to match the success message from `/init-project`. -->

| Spec | Unresolved count | Notes |
|------|------------------|-------|
| [{{slug}}]({{slug}}.md) | {{n}} | {{one-line summary of what is unresolved, or `-`}} |

**Total unresolved across all specs:** {{n}}
