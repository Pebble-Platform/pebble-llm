# Voice / multimodal (capability — STUB, adjacent stream)

> **Status:** stub — an **adjacent research stream** that shares the repo but
> not the ordinal-suicide-risk intent. Authoritative detail lives in
> `docs/voice-*.md` (method selection, pilot/tester guides), the voice kernels
> under `notebooks/`, and `data/voice/`. Not owned by a `001` phase.

**What it covers:** heterogeneous multi-task voice heads (emotion + affect CCC +
crisis recall-floor) and local serving (`.venv-voice`, py3.12, torch 2.2.2 CPU).
Tracked here only so the capability map covers the whole repo; it evolves under
its own task docs, and the intent-layer constraints in this folder bind it only
where they are repo-wide (I4 data ethics, I5 reproducibility).
